language: generic
sudo: required
services:
    - docker


before_install:
    - docker build -t sandeepsangole/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true sandeepsangole/docker-react npm run test -- --coverage